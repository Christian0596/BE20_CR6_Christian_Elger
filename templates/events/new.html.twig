{% extends 'base.html.twig' %}

{% block title %}New Events{% endblock %}

{% block body %}
<div class="container">
<div class="color">
    <h1 class="text-center">Create a new Event</h1>

    {{ include('events/_form.html.twig') }}

    <a href="{{ path('app_events_index') }}" class="btn btn-success w-100">Back</a>
    </div>
</div>
{% endblock %}
